# Bravue Store Android WebView

Android WebView app for https://www.bravue.store/.

## Build

Install Android Studio or a JDK + Android SDK, then run:

```powershell
gradle assembleDebug
```

The debug APK will be generated at:

```text
app/build/outputs/apk/debug/app-debug.apk
```
